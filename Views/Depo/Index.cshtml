@model List<KategoriGetModels>

@{
    Layout = "_AdminLayout"; 
}

<main class="container my-3">
    <div class="row">
        <div class="col">
            <div class="card shadow">
                <div class="card-header py-1 d-flex justify-content-between align-items-center">
                    <h5 class="card-title">Depolar</h5>

                    <a class="btn btn-sm btn-primary" id="ListeYenileBtn">
                        <i class="fa-solid fa-refresh"></i>
                    </a>

                    <a class="btn btn-sm btn-primary" id="AddDepoBtn">
                        <i class="fa-solid fa-plus"></i>
                    </a>
                </div>
                <div class="card-body">


                   <div id="ListePartialViewiGelenDiv"></div>

                    
                </div>
            </div>
        </div>
    </div> 
</main>

<script src="~/depo.js"></script>
 